namespace Q.Delegate.Event;

public class Loader
{
    internal string LoadFiles(string userName) => $"Hello, {userName}! The files are loading...";
    internal string LoadMaps(string userName) => $"Hello, {userName}! The maps are loading...";
    internal string LoadSkins(string userName) => $"Hello, {userName}! The skins are loading...";
}